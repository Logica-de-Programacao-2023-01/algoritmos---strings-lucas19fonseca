package main

import (
	"fmt"
)

func main() {

	var str string

	fmt.Print("Insira uma string: ")
	fmt.Scanln(&str)

	stringinvertida := ""

	for i := len(str) - 1; i >= 0; i-- {
		stringinvertida += string(str[i])
	}
	fmt.Printf("%s na forma invertida fica %s", str, stringinvertida)
}
